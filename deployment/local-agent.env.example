# Local connector runtime environment.
# The connector runs on the same machine/LAN as Tally.
TALLY_URL=http://127.0.0.1:9000

# Set this to the agent_auth_token returned by the backend pairing-token endpoint.
# When set, /tally/execute rejects requests without this token.
LOCAL_AGENT_TOKEN=replace-with-agent-auth-token
